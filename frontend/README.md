# Frontend App

This folder contains the Next.js operator dashboard for LeadFlow.

## Local run

1. Copy `.env.example` to `.env.local`
2. Install deps: `npm install`
3. Start dev server: `npm run dev`
4. Open: `http://localhost:3001`

Backend API base URL is controlled by `NEXT_PUBLIC_API_BASE_URL`.
